# Dashboard — Configuração do Google Cloud
# Copie para .env e preencha os valores (para testes locais com Docker)
# Em produção, estas variáveis são passadas via --set-env-vars no deploy.ps1

GCS_BUCKET=opf-data-bucket
GCS_PREFIX=data/
GOOGLE_CLOUD_PROJECT=opf-dash
